import { LocaleKey } from "./LocaleKey";
export declare class LocalizationMap {
    name: LocaleKey;
    code: string;
    countryCode: string;
    serverSideId: number;
    constructor(name: LocaleKey, code: string, countryCode: string, serverSideId: number);
}
