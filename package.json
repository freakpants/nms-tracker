{
  "name": "nms-tracker",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c auto \"yarn --cwd server dev\" \"yarn --cwd client dev\""
  },
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "axios": "^1.13.4"
  }
}
